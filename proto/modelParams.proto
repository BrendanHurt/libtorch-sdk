syntax = "proto3";

package params;

message modelParams {
    //repeated bytes parameters = 1;
    //swap back to repeated bytes when protobuf's working
    bytes parameters = 1;
    //string tensor_type = 2;
}